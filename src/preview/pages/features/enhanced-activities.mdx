import { DocsPage, Section, Example, PropsTable } from '@/preview/_docs';
import * as Examples from './enhanced-activities.examples';
import examplesSource from './enhanced-activities.examples?raw';

<DocsPage
	title="Features · Enhanced activities"
	description="Shopify-style unified surface: domain events + comments under one chronological timeline. Comments are activities with `event='note'` whose body is rendered via `<MentionContent>` — inline `@`/`#`/`$` chips look identical to the dedicated comments page. Composer at the top uses the same TipTap-backed editor with the live `@`-trigger flow."
	layer="features"
	status="ready"
>

<Section title="Examples" id="examples">

<Example name="APISurface" source={examplesSource}>
	<Examples.APISurface />
</Example>

<Example name="ActivityTimelineComposer" source={examplesSource}>
	<Examples.ActivityTimelineComposer />
</Example>

<Example name="CompactDensity" source={examplesSource}>
	<Examples.CompactDensity />
</Example>

</Section>


<Section title="API" id="api">

#### ActivityFeedCard
<PropsTable component="ActivityFeedCard" />

#### CommentComposer
<PropsTable component="CommentComposer" />

#### MentionContent
<PropsTable component="MentionContent" />

</Section>

</DocsPage>
